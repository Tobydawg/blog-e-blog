

  <section class="comment">
    <div class="meta">
      {{User.username}}
    </div>
    <div class="text">
      {{body}}
    </div>
  </section>

  
{{!-- Danielles code --}}

{{!-- <div class="comments">
{{#if post.comments}}
    <section class="card">
      <div class="card-header">
        <h3>Comments</h3>
      </div>
      <div class="card-body">
        {{#each post.comments}}
          <div>
            <p>
              {{body}}
            </p>
            <p>
              &mdash; {{User.username}}, {{format_date createdAt}}
            </p>
          </div>
        {{/each}}
      </div>
    </section>
  {{/if}}
</div
   --}}
